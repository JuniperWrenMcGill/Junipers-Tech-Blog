<div class="post-cards">
  {{#each posts as |post| }}
    <div class="card">
      <a href="/posts/{{post_id}}">
        <h2>{{post.title}}</h2>
        <p>{{post.description}}</p>
      </a>
    </div>
  {{else}}
    <p>No posts found.</p>
  {{/each}}
</div>
